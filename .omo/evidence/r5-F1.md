# R5-F1 计划合规审计（explore）

- 日期：2026-08-26
- 计划：`.omo/plans/annotation-ux-round5.md`（6 任务均标 [x]）
- 审计方式：只读核查。逐条以 file:line 证据核对 6 TODO + 5 决策 + Must-NOT-Have；交叉验证 `git diff --numstat`、notepad（`.omo/notepads/annotation-ux-round5/{decisions,learnings}.md`）与源码实况。
- 工作区变更面（git diff --numstat）：
  - `UI/frontend/src/views/admin/AnnotationManagement.vue` +179/-47（T1+T2+T3+T5+T6）
  - `UI/frontend/src/components/Sidebar.vue` +13/-1（T4）
  - `UI/frontend/src/router/index.js` +7/-1（T4）
  - `UI/frontend/src/views/AnnotationWorkbench.vue` +4/-1（T4）
  - `UI/backend/tests/utils.py` +24/-0（T5 基建修复）
  - 新增未跟踪：`UI/frontend/src/views/AnnotationHistoryView.vue`（142 行，T4）、`UI/backend/tests/test_annotation_2b_highlight.py`（253 行，T5）

---

## 一、6 任务逐条核对

### [x] T1 复核队列/详情/diff 缺失样式补全（1a+1e）— PASS
全部计划类名已在 `<style scoped>` 落地（`UI/frontend/src/views/admin/AnnotationManagement.vue`）：
- `.review-group-list` L1725；`.review-group-card` L1726-1737；`.review-group-info` L1739；`.review-group-task` L1740；`.review-group-annotator` L1741；`.review-group-table` L1742；`.review-group-count` L1743；`.review-group-time` L1744
- `.review-detail-header` L1747；`.review-detail-title` L1758；`.review-detail-time` L1759
- `.review-flat-list` L1762；`.review-select-all` L1769；`.review-count` L1770；`.rev-summary` L1778；`.core-missing-badge` L1784；`.badge-no-diff` L1791；`.badge-has-diff` L1792
- `.review-expand-row/-cell` L1797-1798；`.diff-legend/-legend-mark` L1800-1801；`.diff-grid` L1802；`.diff-col-title` L1806；`.diff-key` L1810；`.diff-val` L1811
- 复用既有 `.review-toolbar/.review-table/.review-row/.row-selected/.diff-changed`（L1642 区段、L1813）保持不动
- 构建 EXIT=0 见 notepad learnings.md「Build Verification」（1381 modules，CSS 17.47→17.85kB 演进链）；截图留待 F3 QA

### [x] T2 进入复核自动展开 + 多行展开（1b）— PASS
- 标量→Set：`const expandedReviewIds = ref(new Set())` AnnotationManagement.vue L1067；模板 `.has()` 判定 L343/L346；全文件 21 处引用均为 Set 语义，无残留单值引用（grep 验证）
- 进入详情全量展开：`openReviewDetail(group)` L1111-1115 → `new Set(group.items.map(i => i.submission_id))`
- 详情工具栏「全部展开/收起」：按钮 L274-280 + `isAllExpanded` computed L1150-1152 + `toggleAllExpand` L1154-1159
- 刷新裁剪 Set 语义：`loadReviewGroups` 内按 `validIds` 交集过滤 L1093-1095（失效组整体清空 L1100）
- Playwright 断言留待 F3（本审计为静态合规，构建 EXIT=0 已由 learnings.md 记录）

### [x] T3 操作记录三分类（1d）— PASS
- `ACTION_CATEGORIES` 三段映射 AnnotationManagement.vue L1412-1416，与决策 #2 逐字一致
- 分类筛选下拉：L502 `<select v-model="logCategoryFilter">` @change=`handleCategoryChange`（L1461-1466 清理越界 action 筛选后重载）；与现有 action 筛选并存且 AND 叠加（`filteredLogActions` L1451-1453 收窄动作下拉、`displayedLogs` L1456-1459 收窄表格行）
- 分类列/徽章：表体 L547-550 `getCategory(log.action)` + `categoryMeta(...)`；样式 `.badge-category-task/edit/review/neutral` L1850-1853
- 「10 动作均落唯一分类」：LOG_ACTIONS 共 10 项（approve/reject/expire/rollback/save_direct/claim/assign/draft/no_change/submit，L1387-1398），映射 4+4+2 无交叠无遗漏（learnings.md 记录 set 相等验证）。注：仓库无任何前端单测基建（无 vitest/jest，package.json scripts 仅 dev/build/preview），自动化断言需引入新依赖、与决策 #5 冲突——以常量级 set 等价人工核验替代，属合规豁免，非缺陷。

### [x] T4 标注记录移入 sidebar（2a）— PASS
- Sidebar annotator 块新增「标注记录」项：`UI/frontend/src/components/Sidebar.vue` 新增 router-link `to="/annotate/history"`（现 L81-92），工作台链接 active 改精确匹配 `=== '/annotate'` 避免双高亮（L72）
- 路由：`UI/frontend/src/router/index.js` L63-68 新增 `annotate/history` → 懒加载 AnnotationHistoryView；守卫放宽为 `!to.path.startsWith("/annotate")` L122-127
- 只读历史视图：新文件 `UI/frontend/src/views/AnnotationHistoryView.vue`（142 行），复用 `myAnnotationHistory({page, page_size})` API（L54/L66），分页表格 + 返回工作台按钮
- 工作台「我的标注」改跳转：`UI/frontend/src/views/AnnotationWorkbench.vue` L5 `@click="router.push('/annotate/history')"` + `useRouter` 引入；抽屉代码保留避免无关 churn

### [x] T5 无需修改全字段高亮修复 + 空 proposed 不落库锁测（2b）— PASS
- 前端 `diffFields(entry)` 只返回 `Object.keys(entry.proposed_fields || {})`：AnnotationManagement.vue L1166-1168；`hasDiff` L1160-1164
- 空 proposed 渲染占位不渲染 diff 网格：展开行内 `<div v-if="!hasDiff(item)" class="no-diff-placeholder">无需修改</div>` L348，`v-else` 才包 legend+diff-grid L349 起；汇总列徽章 `badge-no-diff` L323；占位样式 L1814
- `diff-changed` 仅对 proposed 键循环内真差异生效：L364-369 绑定 `formatValue(proposed) !== formatValue(current)`，循环域即 proposed_keys（L361），不可能出现 current 全字段高亮
- 后端锁测：新文件 `UI/backend/tests/test_annotation_2b_highlight.py` 两用例——单条审批空 proposed 后核心 title/journal/updated_at 不变 + log old/new={}（L166-216）；批量通过混合场景 item0 不变、item1 正常落库（L219-253）；fast-path 本体核实于 `UI/backend/app/services/annotation_service.py` L1572 `if not submission.proposed_fields:` 分支仅写 submission/item/log、零核心表触碰
- 配套基建：`tests/utils.py` make_db 在 create_all 前剥离 SQLite 不支持的 NOW() server_default/onupdate（+24 行，测试环境专用，PG 语义不受影响）

### [x] T6 向导预览 v-if+v-for 同元素修正 — PASS
- 原 L128-131 `<tr v-for ... v-if>` 已拆分：`<template v-for="item in previewResult.items" :key="item.record_id">` 包裹内层 `<tr v-if="item.eligible" class="wiz-row">`（AnnotationManagement.vue 现 L127-131）；:key 上移 template，checkbox/id/title/badge 全保留内层 tr
- 构建EXIT=0（learnings.md T6 节）；未触碰 expandedReviewIds/diffFields/LOG_ACTIONS/style block

## 二、5 关键决策落实核对

| # | 决策 | 结论 | 证据 |
|---|---|---|---|
| 1 | expandedReviewIds:Set + 进详情全展开 + 全部展开/收起 + 刷新裁剪 Set 化 | 已落实 | L1067 / L1111-1115 / L274-280+L1150-1159 / L1093-1095 |
| 2 | 三分类映射 claim,assign,submit,expire / draft,no_change,save_direct,rollback / approve,reject | 已落实，逐字一致 | L1412-1416 |
| 3 | diffFields 仅 proposed 键 + 空 proposed 占位 + diff-changed 仅真差异 + 后端锁测 | 已落实 | L1166-1168 / L348-349 / L364-369 / test_annotation_2b_highlight.py + service L1572 |
| 4 | sidebar「标注记录」→ /annotate/history + 守卫放行 /annotate 前缀 + 抽屉改跳转 | 已落实 | Sidebar.vue L72,L81-92 / router L63-68,L122-127 / Workbench L5 |
| 5 | 不引依赖 / 不改根 Settings 扁平字段 / .env.example 不新增键 | 全部满足 | 见下节 |

## 三、Must-NOT-Have 核查

1. **无新依赖** — `git status --porcelain -- UI/frontend/package.json UI/frontend/package-lock.json UI/backend/requirements.txt environment.yml` 输出 0 行（四文件零改动）。package.json 实读确认 dependencies 仅 g6/axios/pinia/vue/vue-router、devDependencies 仅 vite/plugin-vue，与基线一致。
2. **不改根 Settings 扁平字段** — `git diff -- UI/backend/app/config.py` 为空（0 行）。config.py 实读：根 Settings 仅含嵌套子配置 postgres/s3/upload/search/auth/annotation + 既有 `cors_allow_origins`（L84-98），无新增扁平字段。
3. **.env.example 稳定** — 未出现在 git status 变更列表；实读 65 行，末段仍为既有 ANNOTATION_ENABLED/TASK_DEADLINE_DAYS/REWORK_DAYS/MAX_PENDING_REWORK，无 round-5 新键。

附加：本轮改动严格收敛于上述 5 修改 + 2 新增文件，无越界源码改动；`git ls-files .omo` 输出空（.omo 未被跟踪，符合约定）。

## 四、观察项（不影响判定）

- T3 的「测试断言」以 set 等价人工核验替代自动化用例：前端无测试框架且决策 #5 禁止新增 dev 依赖，属约束下的合理取舍；若未来引入前端测试基建可补一条常量断言。
- F3（Playwright 手工 QA）与 F4（pytest 全量 225/2/1 + build 退出码）尚未执行，本报告不覆盖运行时证据，静态合规层面无阻断项。

## 五、结论

6/6 任务实现到位，5/5 决策逐字落实，Must-NOT-Have 三项零违反，改动面收敛无泄漏。

VERDICT: APPROVE
